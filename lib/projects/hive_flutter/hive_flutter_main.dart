import 'package:flutter/material.dart';

class HiveFlutterMain extends StatelessWidget {
  const HiveFlutterMain({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
