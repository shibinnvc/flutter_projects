import 'package:flutter/material.dart';

class GoogleSheetMain extends StatelessWidget {
  const GoogleSheetMain({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
